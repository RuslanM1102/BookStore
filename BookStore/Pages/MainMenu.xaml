<Window x:Class="BookStore.MainMenu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookStore"
        mc:Ignorable="d"
        Title="BookStore" Height="450" Width="800" ResizeMode="NoResize" Loaded="Window_Loaded" Closing="Window_Closing" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>

        <Frame x:Name="MainFrame" Grid.Row="1"/>
        <Label Content="{Binding Path=CurrentDateTime}" Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Bottom"/>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Content="Выйти" Width="100" Click="Exit_Click"/>
            <Button Width="100" Click="Export_Click" Content="Экспорт&#xa;издательств"/>
            <Button Width="100" Content="Импорт&#xa;издательств" Click="Import_Click"/>
            <Button Content="Назад" Width="100"/>
        </StackPanel>
    </Grid>
</Window>
