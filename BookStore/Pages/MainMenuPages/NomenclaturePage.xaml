<Page x:Class="BookStore.Pages.MainMenuPages.NomenclaturePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BookStore.Pages.MainMenuPages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="NomenclaturePage">

    <Grid Background="{DynamicResource SubBackground}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="8*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Style="{DynamicResource DefaultButton}" Content="Экспортировать" Margin="10 10 10 0" Click="Export_Click"/>
            <Button Style="{DynamicResource DefaultButton}" Content="Импортировать" Grid.Column="1" Margin="10 10 10 0"/>
            <Button Style="{DynamicResource DefaultButton}" Content="Создать" Grid.Column="2" Margin="10 10 10 0"/>
        </Grid>

        <DataGrid x:Name="Table" Grid.Row="1" Style="{DynamicResource DataGrid}">

        </DataGrid>
    </Grid>
</Page>
